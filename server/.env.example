# 喵迹后端配置（复制本文件为 .env 后填真实值；.env 已被 .gitignore 忽略，不会入库）
PORT=3000
# JWT 签名密钥：务必改成随机长字符串，可用 `openssl rand -hex 32` 生成
JWT_SECRET=change_me_to_a_random_long_secret
# SQLite 数据库路径（默认 ./data/miaoji.db）
DB_PATH=./data/miaoji.db
