# 喵迹 · 云端账号 + 家庭协作 技术方案（V1）

> 决策摘要：一步到位做「账号登录 + 云端数据库 + 家庭邀请（多人共维护一份猫数据）」。
> 周期估算：MVP ≈ 2.5–3.5 周。部署底座：阿里云 ECS 单机。
> 本文只出方案，不写业务代码。确认后进入实施。

---

## 0. 决策摘要（已与沛东确认）

| 项 | 结论 |
|---|---|
| 路线 | 云端 + 账号 + 家庭协作，**一步到位**（不分阶段 A/B） |
| 周期 | ≈ 2.5–3.5 周 MVP |
| 注册方式 | 手机号注册 / 邮箱注册（二选一，MVP）；微信 OAuth 列后期 |
| 视觉模型 | 主推 `qwen-vl-max`；省钱用 `qwen2.5-vl-72b-instruct`（仅改 `VISION_MODEL`，不动前端） |
| 文本模型 | `deepseek-chat`（已验证） |
| AI 代理 | 复用 `local-ai-proxy.mjs`，前端**内置地址、隐藏 workerUrl 设置**（Q2） |
| 部署 | 阿里云 ECS 单机：Node 后端 + RDS + 前端静态站 + nginx |
| 数据迁移 | 全家协作做完后，**清空本地 IndexedDB**，登录从云端拉取 |

---

## 1. 实体关系（ER）

```
User (账号)
  │  1 : N
  ▼
Household (家庭组)  ←── invite_code 邀请加入
  │  1 : N
  ▼
Cat (猫)  ←── household_id 隔离
  │  1 : N
  ▼
Log / Reminder / Chat  ←── 均带 household_id + cat_id
```

- **隔离维度**：业务数据全部按 `household_id` 隔离（服务端强制），`cat_id` 仅做子过滤。
- **共享**：同一 Household 下的所有成员看到同一份猫 + 记录，可读写（MVP 全读写；进阶加 owner/只读角色）。

---

## 2. 云端数据模型（阿里云 RDS，MySQL 8 / PostgreSQL 15）

> 每条业务记录带 `household_id`；`created_by` 记操作人（审计/冲突用）。

### users（账号）
```
id            BIGINT PK
phone         VARCHAR(20) UNIQUE NULL   -- 手机号注册
email         VARCHAR(120) UNIQUE NULL  -- 邮箱注册
password_hash VARCHAR(100)              -- bcrypt
nickname      VARCHAR(40)
wechat_openid VARCHAR(64) NULL         -- 后期 OAuth
created_at    TIMESTAMP
```

### households（家庭组）
```
id         BIGINT PK
name       VARCHAR(60)
owner_id   BIGINT FK -> users.id
invite_code VARCHAR(12) UNIQUE INDEX   -- 加入用
created_at TIMESTAMP
```

### household_members（成员关系）
```
household_id BIGINT FK
user_id      BIGINT FK
role         ENUM('owner','member') DEFAULT 'member'
joined_at    TIMESTAMP
PK(household_id, user_id)
```

### cats（猫档案）
```
id, household_id, name, breed, sex, birth(DATE), weight, neutered,
avatar(可放OSS), created_by, created_at
```

### logs / reminders / chats（业务记录）
```
logs:      id, household_id, cat_id, type, status, note, data(JSON),
           photos(JSON/数组, OSS key), ts, created_by
reminders: id, household_id, cat_id, type, label, cycle(INT天), last_ts,
           care_id, created_by
chats:     id, household_id, cat_id, role('me'|'ai'), text, ts
```
> 原前端 `cats` 字段示例：`{id,name,breed,sex,birth,weight,neutered}`（index.html:507），直接映射。

---

## 3. 前端数据层改造（原 IndexedDB → 云端）

现状（已 grep 确认）：读写统一封装在 `idbAll/idbPut/idbDel`（:225-227），隔离靠前端按 `cat_id` 内存过滤（:529-531）。**store 层无 owner/household 概念**。

改造点：
1. 新增 `api.js` 封装 `apiGet/apiPost/apiPut/apiDel`，统一带 `Authorization: Bearer <token>`。
2. `idbPut/o/idbAll` 改为**可选**离线缓存层；业务写入改为 `apiPost(...)`，`household_id` 由登录态自动注入（不在业务代码里逐个传）。
3. 约 **25+ 处** `idbPut('logs'|'reminders'|'cats'|'chats', ...)`（:507-521、727-744、859-873、913、1250、1351、1367-1372 等）改为走 API，由网关统一加 `household_id`。
4. 读取（:527-531 `idbAll('cats'/'logs'/'reminders'/'chats')`）改为按 `household_id` 拉取后前端按 `cat_id` 过滤（逻辑不变，仅数据源切换）。
5. MVP 可先**纯云端**（去掉本地存储），离线缓存作为进阶；降低冲突复杂度。

---

## 4. API 设计（Node 后端，Express/Fastify）

**鉴权**：JWT。登录/注册返回 token，前端存 `sessionStorage`（防 XSS 比 localStorage 好）。

**隔离**：所有业务查询服务端按 `token → user → household_id` 强制过滤，前端无法越权（即使伪造参数也被服务端拦截）。

```
# 鉴权
POST /api/auth/register        {phone|email, password, nickname}
POST /api/auth/login           {phone|email, password} -> {token, user}
POST /api/auth/wechat          (后期) code -> {token}   # 微信 OAuth
GET  /api/me

# 家庭组
POST /api/households           {name} -> {household_id, invite_code}   # 创建并返回邀请码
POST /api/households/join      {invite_code}                           # 加入
GET  /api/households/:id/members
DELETE /api/households/members/:user_id   # 移除成员(owner)

# 业务资源（均按 household_id 隔离，cat_id 子过滤）
GET    /api/cats
POST   /api/cats
PUT    /api/cats/:id
DELETE /api/cats/:id
GET/POST/PUT/DELETE  /api/logs  /api/reminders  /api/chats  (同构)

# AI（复用现有代理逻辑，合并进同一服务）
POST /api/chat     {q, context?}            # DeepSeek
POST /api/vision   {type, image, calib?}    # 通义视觉 qwen-vl-max
```

> 原前端调 `/chat`、`/vision`（无 `/api` 前缀），网关层兼容两种路径即可（代理已支持 `/chat` 与 `/api/chat` 双路由）。

---

## 5. AI 代理（复用，零前端改动）

- 直接复用 `local-ai-proxy.mjs` 双模型架构（文本 DeepSeek + 视觉通义），密钥只在服务端 `.env`，**绝不下发前端**。
- **Q2 落实**：前端「设置」里的 `workerUrl` 输入框隐藏，改为前端内置常量 `API_BASE = 'https://你的域名'`（高级设置保留自定义）。
- 视觉模型切换：改 `VISION_MODEL` 环境变量（`qwen-vl-max` ↔ `qwen2.5-vl-72b-instruct`），前端无感。

---

## 6. 阿里云部署架构

```
用户浏览器 (喵迹 PWA)
   │  HTTPS
   ▼
[ECS: Alibaba Cloud Linux]
   ├─ nginx         (静态托管 index.html + 反代 /api)
   ├─ Node 服务      (Express: 鉴权 + 业务 API + AI 代理，合并一体)
   └─ (进程管理 pm2 / systemd)
   │
   ├─ RDS          (MySQL/PG：users/households/... 业务表)
   └─ OSS          (猫片存储，可选 Phase2)
域名 + 免费 HTTPS 证书 (阿里云 SSL / Let's Encrypt)
```

- **部署包**：前端静态站 + Node 服务 + `deploy.sh`（pm2 起服务 + nginx 配置拷贝）+ 说明。
- **密钥**：DeepSeek / DashScope Key 仅在 ECS 的 `.env`，沛东自己填（不进对话、不下发）。
- **备份**：RDS 自动备份开启；前端导出仍保留（双保险）。

---

## 7. 注册方式建议

| 方式 | 优点 | 代价 | 建议 |
|---|---|---|---|
| 手机号注册 | 国内最通用；家庭邀请可微信发链接 | 需短信服务商（阿里云短信） | **MVP 候选** |
| 邮箱注册 | 零短信成本，实现最简 | 邮箱验证略麻烦 | **MVP 候选** |
| 微信 OAuth | 最契合养猫家庭群一键登录/分享 | 需微信开放平台**企业资质** | 列后期 |

**推荐**：MVP 先做**手机号 + 邮箱二选一**（任一种即可注册）；微信一键登录作为 Phase2，等用户量起来再接（企业资质门槛高，个人阶段不卡这里）。

---

## 8. 视觉模型推荐（沛东去 DashScope 开通）

- **主推 `qwen-vl-max`**：实测 food 类带 calib 校准后 `confidence=0.6`、JSON 结构完整、中文宠物理解强；精度最高。
- **省钱 `qwen2.5-vl-72b-instruct`**：价格约前者 1/3（输入 0.8 / 输出 2 元/百万 token vs 1.6/4），宠物场景日常够用。
- **切换方式**：在 DashScope 控制台开通对应模型 → 改代理 `VISION_MODEL` 环境变量（或 `.env`）→ 重启代理。前端零改动。
- 建议：先用 `qwen-vl-max` 跑通验收，成本敏感时再切 `qwen2.5-vl-72b-instruct` 对比。

---

## 9. 实施清单与周期（一步到位）

| 阶段 | 内容 | 估时 |
|---|---|---|
| 1 | 后端：鉴权（register/login/JWT）+ 7 张表 CRUD + 隔离 | 1.5–2 周 |
| 2 | 家庭组：创建/邀请码/加入/成员管理/角色 | 4–6 天 |
| 3 | AI 代理合并进服务 + Q2 隐藏 workerUrl | 1–2 天 |
| 4 | 前端：登录页 + 登录态 + 数据层改云端 + 家庭页/邀请 UI | 1 周 |
| 5 | 联调 + 阿里云部署 + 验收 | 3 天 |
| **合计** | | **≈ 2.5–3.5 周** |

---

## 10. 数据迁移 / 清本地

- 全家协作功能做完后，沛东确认「可直接把本地数据清完」：
  - 前端加「退出/切换账号 → 清空本地 IndexedDB」逻辑（或升级时自动清）。
  - 登录后从云端拉取 Household 数据，本地仅作缓存（MVP 可纯云端）。
  - 现有 demo 猫「咪咪」数据：可清空，或作为种子保留（建议清空，避免脏数据混家庭）。

---

## 11. 风险与合规

- **隐私**：猫健康数据属个人敏感信息，RDS 需加密存储 + 传输 HTTPS + 按 household_id 强制隔离。
- **越权**：所有查询服务端按 token→household_id 过滤，前端参数不可信。
- **多设备冲突**：MVP 用「时间戳后写覆盖」；进阶做字段级合并（ operational transform / 版本号）。
- **成本**：AI 按量，建议设每日额度告警（DashScope 控制台可配）。

---

## 12. 验收标准

1. 两人用不同手机号注册，一人建家庭、生成邀请码；另一人输码加入。
2. 任一方新增/修改猫档案或记录，另一方刷新即见（共享同一份数据）。
3. 拍照分析（food/stool）走通义视觉返回结构化 JSON；问答走 DeepSeek。
4. 退出登录后本地数据清空，再次登录从云端恢复。
5. 非家庭成员无法读取对方数据（越权拦截验证）。
